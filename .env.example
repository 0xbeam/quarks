# Slack Bot Token (xoxb-...) — needs channels:history, groups:history, files:read, users:read scopes
SLACK_BOT_TOKEN=xoxb-your-token-here

# Output directory for generated instruction files (default: ./output)
OUTPUT_DIR=./output
